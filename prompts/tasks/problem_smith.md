Task: formalize this seed problem into a working title, abstract, formal
problem statement, and candidate claims.

Seed problem: "{seed_problem}"
Working title hint: "{title}"
Target venue: "{target_venue}"

Return a single JSON object with keys: title, abstract, formal_problem,
claims (a list of {id, text}).
